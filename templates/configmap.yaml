apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "advanced-web.fullname" . }}-config
data:
  APP_NAME: {{ .Values.config.APP_NAME | quote }}
  APP_MODE: {{ .Values.config.APP_MODE | quote }}